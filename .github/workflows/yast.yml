name: YaST integration tests
on: [pull_request]

permissions:
  contents: read

jobs:
  yast:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Run test-yast
        run: make test-yast
